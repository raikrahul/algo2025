
Given a single linked list that consists of ‘R’ and ‘B’ values only,
write an efficient function to find the maximum sequence length of
any color. What are the time and space complexities of your solution?
Function prototype:
int FindMaxSeq(Node head)
Input: R B R B B R R R R B B B R
Output: 4

#[derive(Debug, Clone)]
struct Node {
    val : char,
    next : Option<Box<Node>>,
}
fn find_max_seq(node : &head) -> usize
{
    let mut res : usize =0;
     let mut current_node = &head;
    let mut current_count : usize = 0;
    let mut current_color : Option<char> = None;
    llet mut previous_color: Option<char> = head.as_ref().map(|n| n.val);

    let mut max_count :usize = 0;

    while let Some(node) = current_node {
     current_color = node.val;
     if current_color != previous_color {
          if current_count > max_count {
             max_count = current_count;
          

         }
         current_count = 0;
      }
     
      current_node = &node.next;
     previous_color = current_color;
    current_count +=1;
    }
     
  max_count

}








